[package]
name = "google-ai-studio-json-converter"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }  # Or latest clap version 4.x
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
env_logger = "0.10"                                # Or latest env_logger version
anyhow = "1.0"
regex = "1.10"                                     # Or latest regex version
